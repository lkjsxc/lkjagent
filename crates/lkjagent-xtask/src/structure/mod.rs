use std::path::Path;

pub fn run(_args: &[String], _root: &Path) -> i32 {
    eprintln!("structure failed");
    eprintln!("exit status: 1");
    eprintln!("structure audit is not yet rebuilt for the plan engine");
    1
}
