# XML Parse Helpers

## Purpose

This directory holds paired-tag parser helpers split out of the main parser to
keep ownership narrow and file sizes bounded.

## Table of Contents

- [batch_files.rs](batch_files.rs): `fs.batch_write` repeated files wrapper normalization.
