# GitHub Metadata

## Purpose

This directory holds GitHub-specific metadata. It uses _README.md instead
of README.md so GitHub renders the repository root README.md as the project
overview. The workflow under [workflows/verify.yml](workflows/verify.yml)
runs the compose final gate.
