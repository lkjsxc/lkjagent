# GitHub Metadata

## Purpose

This directory holds GitHub-specific metadata. It uses _README.md instead of
README.md so GitHub renders the repository root README.md as the project
overview. Workflow definitions are added here by
[docs/execution/tasks/xtask-checks.md](../docs/execution/tasks/xtask-checks.md)
once the verification gates exist.
