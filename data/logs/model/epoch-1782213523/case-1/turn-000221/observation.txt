<observation>
<status>ok</status>
<content>
artifact next batch
root=structured-output
kind=artifact
missing=3
next_paths:
- operations/recovery.md
- operations/running.md
- operations/verification.md
required_sections:
- title
- purpose
- concrete content
- verification notes
next_action=fs.batch_write
valid_example:
<act>
<tool>fs.batch_write</tool>
<files>
path: structured-output/operations/recovery.md
content:
# Recovery

## Concrete Record

Recovery defines the artifact role, the observed constraints, the file paths it governs, and the decision that makes the page useful for lookup. It gives named examples, operational checks, and verification evidence tied to this exact path.

## Examples And Checks

Example one names a path, an invariant, and the command or audit that proves it. Example two names a failure mode, the repair owner, and the evidence needed before completion.

-- lkjagent-next-file --
path: structured-output/operations/running.md
content:
# Running

## Concrete Record

Running defines the artifact role, the observed constraints, the file paths it governs, and the decision that makes the page useful for lookup. It gives named examples, operational checks, and verification evidence tied to this exact path.

## Examples And Checks

Example one names a path, an invariant, and the command or audit that proves it. Example two names a failure mode, the repair owner, and the evidence needed before completion.

-- lkjagent-next-file --
path: structured-output/operations/verification.md
content:
# Verification

## Concrete Record

Verification defines the artifact role, the observed constraints, the file paths it governs, and the decision that makes the page useful for lookup. It gives named examples, operational checks, and verification evidence tied to this exact path.

## Examples And Checks

Example one names a path, an invariant, and the command or audit that proves it. Example two names a failure mode, the repair owner, and the evidence needed before completion.

</files>
</act>
</content>
</observation>