# Workspace Brief

## Purpose

This generated workspace belongs to lkjagent runtime tasks. Treat existing
files as evidence, not as proof of completion.

## Rules

- Preserve owner objectives and do not mix unrelated topic roots.
- Prefer small batches over large single writes.
- Every generated directory needs a README that links immediate children.
- Every completion claim needs current artifact or document audit evidence.
- Mark scaffold-only, README-only, or drifted content as weak evidence.
- Keep temporary files outside this tree unless a tracked fixture owns them.

## Current Evidence

- `structured-output/` is diagnostic failed-output evidence with scaffold and
  topic-drift findings.
- `docs/` is generated documentation evidence that still requires structure
  audit before completion.
- `generate_random_password.sh` is a generated shell artifact and does not
  satisfy documentation-output objectives.
