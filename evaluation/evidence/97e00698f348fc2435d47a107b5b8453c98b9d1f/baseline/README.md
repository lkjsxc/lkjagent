# Baseline Evidence

## Purpose

Record a sanitized synthetic observation of the production public CLI at source
`97e00698f348fc2435d47a107b5b8453c98b9d1f`.

The copied release binary remained alive for 901 observed seconds. The synthetic
request deterministically routed to a blocked unsupported executor before any
model call, runtime decision, effect, or check. The requested target file stayed
byte-for-byte unchanged. This bundle records failure evidence, not completion.

No SQLite file, endpoint value, credential, authorization data, raw provider
traffic, raw environment, or unnecessary process identifier is attached.
